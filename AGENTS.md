# AGENTS.md — E-AI Office Assistant 开发规则

> 本文件是 AI 在本项目开发时必须遵守的规则。**每次开工前必须完整读取本文件。**
> 架构师（梁政）制定规则并负责验收；AI 负责按规则实现。违反规则即失败。

## 0. 铁律（违反直接失败）

1. **按模块开发**：当前只能做开发计划（`docs/01_开发计划.md`）中指定的当前阶段内容，禁止提前实现后续模块、禁止"顺手优化"无关功能。
2. **技术栈锁定**：只允许使用 `地基卡.md` 锁定的技术栈。禁止擅自引入新依赖、新框架、新特性；确需新增必须先征求架构师同意。
3. **必须可运行**：交付的代码必须在 Windows + Python 3.11 + Node 24 上直接可运行。交付时给出：安装命令、启动命令、访问地址。
4. **不编造**：禁止编造测试结果、禁止假装功能"已完成"。无法验证的必须明确标注"未验证"。
5. **安全底线**：API Key 等敏感信息只放 `.env`（已 gitignore），禁止硬编码进代码；禁止提交 `.env`、数据库、上传文件。
6. **测试三遍**：每次交付的功能，必须**连续完整测试 3 遍**，3 遍全部通过才算完成，才允许提交给架构师确认；3 遍中出现任何一次失败，必须修复后重新测满 3 遍。测试命令和 3 次结果必须如实贴在交付说明里。

## 0.1 底层开发思维（杜绝闭门造车）

1. 启动任何新功能或模块前，优先去 GitHub 查找同类项目、文档、实现思路和源码；参考项目放在 `reference/`。
2. 有成熟现成项目时，优先评估 **license（许可证）、维护状态、安全风险和适配成本** 后复用；禁止直接复制未经许可的代码。
3. 简单 bug 修复、明确小改动或离线任务可不强制调研。
4. 多检索、多看、多拆解开源项目，可复用内容直接取用，有参考价值的方案借鉴参考。
5. 多套开源方案交叉对比，取优势、剔除缺陷。

## 0.2 文档同步规则

1. 功能、业务逻辑、接口协议、数据结构、产品交互或模块职责变化时，必须同步检查 README、AGENTS、地基卡、开发计划、技术架构等文档是否过期。
2. 文档同步是收尾的一部分：准备说明"完成"、测试通过或 commit 前，都要检查文档。
3. 内部实现细节调整且不影响使用方式/接口/数据/交互/模块边界时，可说明"无需更新文档"。
4. 旧文档会误导后续开发时，必须更新或归档，不能让过期文档留在默认阅读路径。
5. 更新当前执行文档时，正文改成当前真实规则，不要用"修订说明/当前改为"这类补丁式语气。
6. 只有归档、changelog 或用户明确要求保留历史时才写历史修订说明。

## 1. 每次交付必须包含

- 修改/新增了哪些文件
- 验证命令 + 验证结果（贴真实输出）
- 未验证的风险点

## 2. 代码规范

- 后端目录结构按 `地基卡.md` 锁定，新增文件必须落在对应 feature 目录，禁止在根目录乱建 `utils/helper/temp` 等杂散文件。
- 单文件超过 400 行必须拆分；超过 800 行禁止继续堆功能。
- 关键代码必须有中文注释；每个模块必须能在 `README` 或对应文档找到"怎么跑、怎么验"。
- 小步交付：每完成一个可运行的子功能就停下，给出运行命令让架构师验证，通过后再继续。
- 不"顺手重构"无关代码；一个任务只做一个目标。

## 3. AI 调用规则（后端统一走 AI 适配层）

- 所有大模型调用必须通过 `backend/app/ai/` 下的适配层，禁止在业务代码里直接拼 API。
- 提示词模板集中放 `backend/app/ai/prompts.py`，模板锁定：
  - 知识库问答：只依据检索片段回答，答不出就直说，禁止编造；输出带来源。
  - 会议纪要：按"摘要 / 关键决定 / 待办事项 / 发言要点"四段固定输出 Markdown。
  - 客户提取：只输出 JSON `{name, company, phone, email, notes}`，缺省填"未提供"。
  - 所有调用：temperature=0.2，中文回答，超时30s，失败重试2次。

## 4. 数据与测试

- 开发测试使用临时 SQLite/临时目录，禁止污染正式数据库。
- 核心函数至少给出 1 个输入输出示例供架构师验证。

## 5. 文档同步

- 功能、接口、数据结构、交互变化时，必须同步更新对应文档（README / 地基卡 / 开发计划 / 技术架构）。
- 验收通过的阶段，在 `docs/01_开发计划.md` 勾选完成状态。
