# 项目地基卡（技术栈锁定，AI 不得擅改）

> 填表人：梁政（架构师）｜ 状态：锁定 ｜ 变更需架构师书面同意

## 1. 业务目标
给传统企业用的"AI 办公助手"，把知识库、会议纪要、客户数据三件事从手工变成 AI 自动。**面试可现场演示、可部署上线。**

## 2. 技术栈（锁定）

| 层 | 选型 | 版本 | 说明 |
|----|------|------|------|
| 后端 | Python + FastAPI + Uvicorn | 3.11 / 0.115 / 0.34 | 唯一后端，禁止换 Django/Flask |
| ORM | SQLAlchemy 2 | 2.0 | 数据库访问 |
| 数据库 | SQLite | 内置 | 开发期零运维；接口层已抽象可切 MySQL |
| 向量库 | 自实现（numpy 余弦 + SQLite BLOB） | 本机 chromadb 跨进程加载索引异常，故轻量实现；可平滑迁移 chromadb/Milvus |
| 大模型 | DeepSeek API（deepseek-chat） | — | 走 OpenAI 兼容接口，AI 适配层统一封装 |
| 文档解析 | pypdf / python-docx / openpyxl / python-multipart | — | 模块A/C 用 |
| 前端 | React 18 + Vite + Ant Design + Axios | — | 唯一前端 |
| 部署 | Docker Compose + 轻量云服务器（后期） | — | 开发期本地运行 |

## 3. 目录结构（锁定）

```
EAI-Office-Assistant/
├── README.md / AGENTS.md / 地基卡.md
├── backend/
│   ├── app/
│   │   ├── main.py            # FastAPI 入口
│   │   ├── config.py          # 配置（读 .env）
│   │   ├── models.py          # SQLAlchemy 模型
│   │   ├── routers/           # kb.py / meeting.py / customer.py / stats.py
│   │   ├── ai/                # llm.py(DeepSeek封装) / prompts.py(提示词模板)
│   │   └── rag/               # 切片/向量化/检索
│   ├── tests/                 # 后端测试
│   ├── requirements.txt
│   └── .env.example
├── frontend/
│   └── src/                   # React（阶段0创建）
├── docs/                      # 项目文档
└── scripts/                   # 维护脚本
```

## 4. 核心数据模型

```
documents(id, title, file_type, created_at)
kb_chunks(id, doc_id, chunk_text)          # 向量存 chromadb
meetings(id, title, transcript, summary, decisions, todos, created_at)
customers(id, name, company, phone, email, source, notes, created_at)
```

## 5. 关键 API（锁定）

```
POST /api/kb/upload         # 上传文档
POST /api/kb/ask            # 提问 {question} -> {answer, sources[]}
POST /api/meeting/parse     # 粘贴文字稿 -> 结构化纪要
POST /api/customer/import   # 导入 Excel
POST /api/customer/extract  # 文本 AI 提取客户
GET  /api/customer/search?q=# 搜索
GET  /api/stats/dashboard   # 看板统计
GET  /api/health            # 健康检查
```

## 6. 验收标准（每个模块）
"做完" = 在网页上能操作 + 后端接口有真实输入输出 + 有运行命令。

## 7. 禁止事项
- 禁止引入 Kafka/Flink/Hadoop/Elasticsearch 等重中间件
- 禁止用 Flask/Django 替换 FastAPI
- 禁止把 .env/数据库/上传文件提交进 Git
- 禁止一次性生成整个模块，必须小步交付
- 禁止编造测试结果和"已完成"
